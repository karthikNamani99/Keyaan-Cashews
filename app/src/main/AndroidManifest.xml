<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.keyaancashews">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="com.google.android.gms.wallet.api.enabled"
            android:value="true" />

        <activity android:name=".activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.ShoppingActivity" />
        <activity android:name=".activities.SubCategoryList"
            android:label="White Wholes"/>
        <activity android:name=".activities.ProductActivity" />
        <activity android:name=".activities.CheckoutActivity" />
        <activity android:name=".activities.PaymentsActivity" />
        <activity android:name=".activities.PayPalCheckoutActivity">
        </activity>
        <activity android:name=".activities.Register_Page"
            android:theme="@style/Theme.AppCompat.NoActionBar"/>
        <activity android:name=".activities.Login_Page"
            android:theme="@style/Theme.AppCompat.NoActionBar"/>
    </application>

</manifest>